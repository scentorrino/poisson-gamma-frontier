% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/halfnormal.R
\name{efficiency_scores_halfnormal}
\alias{efficiency_scores_halfnormal}
\title{Posterior efficiency scores for the Poisson / half-normal frontier}
\usage{
efficiency_scores_halfnormal(fit, y, X, R = 500)
}
\arguments{
\item{fit}{A \code{"poisson_halfnormal"} object.}

\item{y}{Integer vector of outcomes.}

\item{X}{Model matrix used at fit time.}

\item{R}{Number of Halton draws (default 500).}
}
\value{
A \code{data.frame} with columns \code{i}, \code{eff_score},
  \code{eff_lower}, \code{eff_upper} (95\% interval, clipped to [0, 1]).
}
\description{
Companion to \code{\link{efficiency_scores}} for fits returned by
\code{\link{fit_poisson_halfnormal}}.  Computes simulated posterior moments
\eqn{E[e^{-u_i}|y_i]} and \eqn{E[e^{-2u_i}|y_i]} via Halton draws of the
half-normal prior, weighted by the Poisson likelihood (importance
sampling).
}
\examples{
\donttest{
set.seed(1L)
n <- 100L
X <- cbind(1, rnorm(n))
u <- abs(rnorm(n, sd = 0.5))
y <- rpois(n, lambda = exp(drop(X \%*\% c(1, 0.5)) - u))
fit <- fit_poisson_halfnormal(y, X, R = 100L)
head(efficiency_scores_halfnormal(fit, y, X, R = 200L))
}

}
